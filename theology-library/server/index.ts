import express from 'express'; import cors from 'cors'; import path from 'path'; import fs from 'fs'; import multer from 'multer'; import { getDb } from './db/connection'; import categories from './routes/categories'; import subtopics from './routes/subtopics'; import testaments from './routes/testaments'; import books from './routes/books'; import chapters from './routes/chapters'; import imageSets from './routes/imageSets'; import images from './routes/images'; import videos from './routes/videos'; import texts from './routes/texts'; import crossLinks from './routes/crossLinks'; import search from './routes/search';
export async function startServer(port:number,dataPath:string){ const app=express(); const db=getDb(dataPath); const imgDir=path.join(dataPath,'images'); fs.mkdirSync(imgDir,{recursive:true}); const upload=multer({dest:imgDir}); app.use(cors({origin:'http://localhost:5173'})); app.use(express.json()); app.use((req,res,next)=>{(req as any).db=db;(req as any).upload=upload;next();}); app.get('/api/health',(_,res)=>res.json({ok:true})); app.use('/api/categories',categories); app.use('/api/subtopics',subtopics); app.use('/api/testaments',testaments); app.use('/api/books',books); app.use('/api/chapters',chapters); app.use('/api/image-sets',imageSets); app.use('/api/images',images); app.use('/api/videos',videos); app.use('/api/texts',texts); app.use('/api/cross-links',crossLinks); app.use('/api/search',search); await new Promise<void>(r=>app.listen(port,()=>r())); }
